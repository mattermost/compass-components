import quartz from './theme.quartz';
import createTheme from './create-theme';
import type { TTheme } from './theme.types';

const lightTheme: TTheme = createTheme(quartz);

export default lightTheme;
