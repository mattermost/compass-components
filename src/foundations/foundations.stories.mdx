import { Description, Meta, Subtitle, Title } from '@storybook/addon-docs/blocks';

import colorStoryParams from './colors/colors.stories.mdx';
import typographyStoryParams from './typography/Typography.stories.mdx';
import shapeStoryParams from './shape/Shape.stories.mdx';

import { Utils } from '../utils';
import Shape from './shape';
import Typography from './typography';
import Grid from './layout/Grid';
import ThemeProvider from './theme-provider/theme-provider';

import collageImg from '../assets/images/foundation_overview_collage.png';
import colorImg from '../assets/images/foundation_overview_color.png';
import typographyImg from '../assets/images/foundation_overview_typography.png';
import shapeImg from '../assets/images/foundation_overview_shape.png';
import iconsImg from '../assets/images/foundation_overview_icons.png';
import spacing from './layout/Spacing';

<Meta title="Foundations/Overview" />

<ThemeProvider />

<Title>Foundations</Title>
<Subtitle>
    Foundations are the most basic building blocks of the system. <br />
    Every interface element uses these foundations in some form or another.
</Subtitle>

<img src={collageImg} alt={'foundation-collage'} />

<Description>
    Think of foundations as a base layer of style definition for the system. Foundatiofixed a bug ns
    include things like Color, Typography, and Iconography.
</Description>

<Grid row>
    <Grid padding={spacing().all(75).only('left', 0)}>
        <a href={Utils.getStoryDocumentationUrl(colorStoryParams)} target={'_self'}>
            <Shape border borderRadius={4} elevation={0} elevationOnHover={2}>
                <img src={colorImg} alt={'foundation-colors'} />
                <Grid padding={spacing().vertical(50).horizontal(100)}>
                    <Typography variant={'h3'} size={300}>
                        Color
                    </Typography>
                    <Typography variant={'body'} size={100}>
                        Discover the various palettes and shades.
                    </Typography>
                </Grid>
            </Shape>
        </a>
    </Grid>
    <Grid padding={spacing().all(12)}>
        <a href={Utils.getStoryDocumentationUrl(typographyStoryParams)} target={'_self'}>
            <Shape border borderRadius={4} elevation={0} elevationOnHover={2}>
                <img src={typographyImg} alt={'foundation-typography'} />
                <Grid padding={spacing().vertical(50).horizontal(100)}>
                    <Typography variant={'h3'} size={300}>
                        Typography
                    </Typography>
                    <Typography variant={'body'} size={100}>
                        Guidelines on typefaces, scales, weights and more.
                    </Typography>
                </Grid>
            </Shape>
        </a>
    </Grid>
    <Grid padding={spacing().all(12)}>
        <a href={Utils.getStoryDocumentationUrl(shapeStoryParams)} target={'_self'}>
            <Shape border borderRadius={4} elevation={0} elevationOnHover={2}>
                <img src={shapeImg} alt={'foundation-shape'} />
                <Grid padding={spacing().vertical(50).horizontal(100)}>
                    <Typography variant={'h3'} size={300}>
                        Shape
                    </Typography>
                    <Typography variant={'body'} size={100}>
                        How shape is used in Mattermost interfaces
                    </Typography>
                </Grid>
            </Shape>
        </a>
    </Grid>
    <Grid padding={spacing().all(12).only('right', 0)}>
        <a href={Utils.getStoryDocumentationUrl(colorStoryParams)} target={'_self'}>
            <Shape border borderRadius={4} elevation={0} elevationOnHover={2}>
                <img src={iconsImg} alt={'foundation-icons'} />
                <Grid padding={spacing().vertical(50).horizontal(100)}>
                    <Typography variant={'h3'} size={300}>
                        Iconography
                    </Typography>
                    <Typography variant={'body'} size={100}>
                        Simple visual aids used to guide our users
                    </Typography>
                </Grid>
            </Shape>
        </a>
    </Grid>
</Grid>
