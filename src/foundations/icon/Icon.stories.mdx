import { Meta, Story, Canvas, ArgsTable, Title, Subtitle } from '@storybook/addon-docs/blocks';

import Icon from './Icon';
import { ICON_GLYPHS, ICON_SIZES } from './Icon.constants';

<Meta component={Icon} title="Foundations/Icon" />

<Title>Icon</Title>

## Component API

<ArgsTable of={Icon} />

export const Template = (args) => {
    return <Icon {...args} />;
};

<Canvas hidden>
    <Story
        name="Default"
        args={{
            size: 20,
            glyph: 'mattermost',
        }}
        argTypes={{
            size: {
                control: {
                    type: 'select',
                    options: ICON_SIZES,
                },
            },
            glyph: {
                control: {
                    type: 'select',
                    options: Object.keys(ICON_GLYPHS),
                },
            },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
