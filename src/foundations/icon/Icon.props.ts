import { TIconGlyph, TIconSize } from './Icon.types';

type PIcon = {
    className?: string;
    glyph?: TIconGlyph;
    size?: TIconSize;
};

type PStyledIcon = {};

export type { PIcon, PStyledIcon };
