import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import GlobalStyle from '../../global-styles/globalStyles';
import Grid from '../layout/Grid';

import { createColorShades } from '../../utils';
import Swatch from './Swatch';
import neutralColors from './default-theme/neutral';
import blueColors from './default-theme/blue';
import cyanColors from './default-theme/cyan';
import tealColors from './default-theme/teal';
import yellowColors from './default-theme/yellow';
import redColors from './default-theme/red';
import orangeColors from './default-theme/orange';
import purpleColors from './default-theme/purple';
import indigoColors from './default-theme/indigo';
import greenColors from './default-theme/green';

<Meta title="Foundations/Palette" />

# Color

> Color is a fundamental building block of any interface and is an integral part of how a product feels.
> Consistent application of color also helps users learn an interface.

## Palettes

### Primary Palette

Our Primary Palette is comprised of blue, indigo, and neutral tones to bring trust and boldness to our brand. We use
these colors in logical ways to guide the eye and highlight important information.

<Grid row justify={'start'}>
    <Swatch color={blueColors[500]} colorName={'Blue'} shade={500} variant={'bottom'} />
    <Swatch color={indigoColors[500]} colorName={'Indigo'} shade={500} variant={'bottom'} />
    <Swatch color={neutralColors[1150]} colorName={'Neutral'} shade={1150} variant={'bottom'} />
    <Swatch color={neutralColors[0]} colorName={'Neutral'} shade={0} variant={'bottom'} />
</Grid>

### Secondary Palette

Our secondary palette has a variety of colors to keep things fresh and interesting. We add warmer colors throughout to
soften the experience and to impart confidence and optimism.

<Grid row justify={'start'}>
    <Swatch color={cyanColors[400]} colorName={'Cyan'} shade={400} variant={'bottom'} />
    <Swatch color={purpleColors[400]} colorName={'Purple'} shade={400} variant={'bottom'} />
    <Swatch color={tealColors[400]} colorName={'Teal'} shade={400} variant={'bottom'} />
    <Swatch color={greenColors[500]} colorName={'Green'} shade={500} variant={'bottom'} />
    <Swatch color={orangeColors[400]} colorName={'Orange'} shade={400} variant={'bottom'} />
    <Swatch color={yellowColors[400]} colorName={'Yellow'} shade={400} variant={'bottom'} />
    <Swatch color={redColors[400]} colorName={'Red'} shade={400} variant={'bottom'} />
</Grid>

## Semantic Colors

Semantic colors help users identify status, see actions, locate help, and understand the next steps they need to take.
The consistent use of color keeps the cognitive load low and makes for a unified and engaging user experience.

> **NOTE:** These colors are shades of the corresponding Secondary Palette above.

<Grid justify={'start'}>
    <Swatch color={redColors[500]} colorName={'Alert: Red'} shade={500} />
    <Swatch color={orangeColors[500]} colorName={'Warning: Orange'} shade={500} />
    <Swatch color={greenColors[600]} colorName={'Success: Green'} shade={600} />
    <Swatch color={purpleColors[600]} colorName={'Info: Purple'} shade={600} />
</Grid>

## Shades

Each of the Primary and Secondary colors have shades ranging from 100-800 with the exception of Neutrals which have a
wider range of shades. See below for details.

### Neutral Colors

Neutral colors have an increased level of variance to be used for text, elevations, and subtle backgrounds. We also use
neutrals when we don't want to draw too much attention to a particular touchpoint or convey information such as a
disabled option.

<Grid justify={'start'}>
    {Object.keys(neutralColors).map(shade => (
        <Swatch key={`neutralColor_${shade}`} color={neutralColors[shade]} colorName={'Neutral'} shade={shade} />
    ))}
</Grid>

### Blue

<Grid justify={'start'}>
    {Object.keys(blueColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={blueColors[shade]} colorName={'Blue'} shade={shade} />
    ))}
</Grid>

### Cyan

<Grid justify={'start'}>
    {Object.keys(cyanColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={cyanColors[shade]} colorName={'Cyan'} shade={shade} />
    ))}
</Grid>

### Indigo

<Grid justify={'start'}>
    {Object.keys(indigoColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={indigoColors[shade]} colorName={'Indigo'} shade={shade} />
    ))}
</Grid>

### Purple

<Grid justify={'start'}>
    {Object.keys(purpleColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={purpleColors[shade]} colorName={'Purple'} shade={shade} />
    ))}
</Grid>

### Teal

<Grid justify={'start'}>
    {Object.keys(tealColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={tealColors[shade]} colorName={'Teal'} shade={shade} />
    ))}
</Grid>

### Green

<Grid justify={'start'}>
    {Object.keys(greenColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={greenColors[shade]} colorName={'Green'} shade={shade} />
    ))}
</Grid>

### Red

<Grid justify={'start'}>
    {Object.keys(redColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={redColors[shade]} colorName={'Red'} shade={shade} />
    ))}
</Grid>

### Orange

<Grid justify={'start'}>
    {Object.keys(orangeColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={orangeColors[shade]} colorName={'Orange'} shade={shade} />
    ))}
</Grid>

### Yellow

<Grid justify={'start'}>
    {Object.keys(yellowColors).map(shade => (
        <Swatch key={`blue_${shade}`} color={yellowColors[shade]} colorName={'Yellow'} shade={shade} />
    ))}
</Grid>

export const argTypes = {
    color: {
        table: {
            disable: true,
        },
    },
    name: {
        table: {
            disable: true,
        },
    },
};

export const Template = args => {
    const colors = createColorShades(args.color);
    return (
        <>
            <GlobalStyle />
            <Grid row>
                <Grid>
                    {colors &&
                        Object.keys(colors).map(shade => (
                            <Swatch
                                key={`color_${shade}`}
                                color={colors[shade]}
                                colorName={args.name || ''}
                                shade={shade}
                            />
                        ))}
                </Grid>
            </Grid>
        </>
    );
};

<Canvas hidden>
    <Story
        name="Custom Color"
        argTypes={{
            color: { control: 'color' },
        }}
        args={{
            color: '#14E7D1',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Blue"
        argTypes={argTypes}
        args={{
            name: 'Blue',
            color: '#1C58D9',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Cyan"
        argTypes={argTypes}
        args={{
            name: 'Cyan',
            color: '#1592E0',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Indigo"
        argTypes={argTypes}
        args={{
            name: 'Indigo',
            color: '#1E325C',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Purple"
        argTypes={argTypes}
        args={{
            name: 'Purple',
            color: '#484FAD',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Teal"
        argTypes={argTypes}
        args={{
            name: 'Teal',
            color: '#1ADBDB',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Green"
        argTypes={argTypes}
        args={{
            name: 'Green',
            color: '#3DB887',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Red"
        argTypes={argTypes}
        args={{
            name: 'Red',
            color: '#C43133',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Orange"
        argTypes={argTypes}
        args={{
            name: 'Orange',
            color: '#E07315',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Yellow"
        argTypes={argTypes}
        args={{
            name: 'Yellow',
            color: '#7A5600',
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
