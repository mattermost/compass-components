import { Meta, Story, Canvas, ArgsTable, Title, Subtitle } from '@storybook/addon-docs/blocks';

import GlobalStyle from '../theme-provider/global-styles/globalStyles';
import Typography from './Typography';
import Grid from '../layout/Grid';
import Shape from '../shape';
import ThemeProvider from '../theme-provider/theme-provider';

<ThemeProvider />

<Meta component={Typography} title="Foundations/Typography" />

<GlobalStyle />

<Title>Typography</Title>

<Subtitle>
    Text an integral part of any user interface, so it’s important that all typographic elements are designed for
    legibility and consistency. This section defines the typographic style and usage guidelines.
</Subtitle>

## Typefaces

Compass uses Metropolis and Open Sans as the primary typefaces. Metropolis is a modern, geometric typeface that is
open-sourced, and openly available. It has been designed for optimal readability at small point sizes while remaining
beautiful at large point sizes.

**Metropolis is used for headings 300-700. See below for more details.**

Open Sans is another modern sans-serif typeface that is incredibly legible and offers an excellent reading experience.
It was developed to have high performance as a web font and is considered to be a font that combines all of the
strongest attributes of the top sans serif fonts. Open Sans is available to be used freely in products or projects.

**Open Sans is used for all body copy and headings 50-200. See below for more details.**

## Type Scale

### Scale Ratio

A type scale has been chosen to ensure that different sizes of text can work together harmoniously. Font sizes have a
scale of 1.125. This means that each size is multiplied or divided by 1.125 from the previous size, starting with the
base size, and then rounded.

## Base Font Size

The base font size for the Mattermost Web App is 14px—also known as the base size '100' in our typographic scale.

### Line Heights

Body copy line heights generally use a multiplier of 1.5 based on the font size, but some adjustmentshave been made
in order for the baselines to fall on the 4px grid.

Heading line heights generally use a multiplier of 1.3 based on the font size, but some adjustments have been made in
order for the baselines to fall on the 4px grid. Smaller headings also use the 1.5 multiplier.

## Headings

### Font Weights

Headings are generally used in the semibold font-weight, but can also be used in regular or light weights in special
cases where the typographic contrast is needed.

### Heading Sizes

The chart below shows all the heading sizes from Heading 100 to Heading 700, where 100 is the base font size as
described above.

export const smallSizes = [
    { size: 25, fontSize: 10, lineHeight: 16 },
    { size: 50, fontSize: 11, lineHeight: 16 },
    { size: 75, fontSize: 12, lineHeight: 16 },
    { size: 100, fontSize: 14, lineHeight: 20 },
    { size: 200, fontSize: 16, lineHeight: 24 },
];
export const headingSizes = [
    ...smallSizes,
    { size: 300, fontSize: 18, lineHeight: 24 },
    { size: 400, fontSize: 20, lineHeight: 28 },
    { size: 500, fontSize: 22, lineHeight: 28 },
    { size: 600, fontSize: 25, lineHeight: 30 },
    { size: 700, fontSize: 32, lineHeight: 40 },
    { size: 1000, fontSize: 40, lineHeight: 48 },
];

export const bodySizes = [...smallSizes, { size: 300, fontSize: 18, lineHeight: 28 }];

<>
    {headingSizes.map(heading => (
        <>
            <hr />
            <Grid justify={'stretch'}>
                <Grid row>
                    <Typography variant={'h3'}>{`Heading ${heading.size}${
                        heading.size === 100 ? ' (Base size)' : ''
                    }`}</Typography>
                </Grid>
                <Grid row>
                    <Grid flex={3}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Family
                        </Typography>
                        <Typography variant={'body'} size={200} gutter={'bottom'}>
                            {heading.size > 300 ? 'Metropolis (600)' : 'OpenSans (600)'}
                        </Typography>
                    </Grid>
                    <Grid flex={2}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Size
                        </Typography>
                        <Typography variant={'body'} size={200} gutter={'bottom'}>
                            {`${heading.fontSize}px`}
                        </Typography>
                    </Grid>
                    <Grid flex={2}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Line Height
                        </Typography>
                        <Typography variant={'body'} size={200} gutter={'bottom'}>
                            {`${heading.lineHeight}px`}
                        </Typography>
                    </Grid>
                    <Grid flex={6}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Example
                        </Typography>
                        <Typography variant={'h1'} size={heading.size} gutter={'bottom'}>
                            The quick brown fox jumps over the lazy dog
                        </Typography>
                    </Grid>
                </Grid>
            </Grid>
        </>
    ))}
</>

## Body

### Font Weights

Body always uses the regular font-weight, but where emphasis is needed, the semi-bold weight can be used.

### Body Sizes

The chart below shows all the body copy sizes where Body 100 is the base font size.

<>
    {bodySizes.map(body => (
        <>
            <hr />
            <Grid justify={'stretch'}>
                <Grid row>
                    <Typography variant={'h3'}>{`Body ${body.size}${
                        body.size === 100 ? ' (Base size)' : ''
                    }`}</Typography>
                </Grid>
                <Grid row>
                    <Grid flex={3}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Family
                        </Typography>
                        <Typography variant={'body'} size={200} gutter={'bottom'}>
                            OpenSans (400)
                        </Typography>
                    </Grid>
                    <Grid flex={2}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Size
                        </Typography>
                        <Typography variant={'body'} size={200} gutter={'bottom'}>
                            {`${body.fontSize}px`}
                        </Typography>
                    </Grid>
                    <Grid flex={2}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Line Height
                        </Typography>
                        <Typography variant={'body'} size={200} gutter={'bottom'}>
                            {`${body.lineHeight}px`}
                        </Typography>
                    </Grid>
                    <Grid flex={6}>
                        <Typography variant={'body'} color={'secondary'} size={50} gutter={'none'}>
                            Example
                        </Typography>
                        <Typography variant={'body'} size={body.size} gutter={'bottom'}>
                            The quick brown fox jumps over the lazy dog
                        </Typography>
                    </Grid>
                </Grid>
            </Grid>
        </>
    ))}
</>

## Font Stack

In cases where standard typefaces are not available, the following font stacks should be used for fallback so the device
uses the system standard `sans-serif` typeface.

```css
font-family: Metropolis, sans-serif;
font-family: “Open Sans”, sans-serif;
```

## Component API

<ArgsTable of={Typography} />

export const Template = args => {
    const { text } = args;
    return (
        <ThemeProvider>
            <Typography {...args}>{text}</Typography>
        </ThemeProvider>
    );
};

export const ParagraphTemplate = args => (
    <ThemeProvider>
        <Shape width={400}>
            <Typography
                size={args.headingSize}
                gutter={args.headingGutter}
                variant={args.headingVariant}
                useRegular={args.headingUseRegular}
                color={args.headingColor}
            >
                {args.heading}
            </Typography>
            <Typography
                size={args.subtitleSize}
                gutter={args.subtitleGutter}
                variant={args.subtitleVariant}
                color={args.subtitleColor}
            >
                {args.subtitle}
            </Typography>
            <Typography
                size={args.paragraphSize}
                gutter={args.paragraphGutter}
                variant={args.paragraphVariant}
                color={args.paragraphColor}
            >
                {args.paragraph}
            </Typography>
            <Typography
                size={args.subtitleSize}
                gutter={args.subtitleGutter}
                variant={args.subtitleVariant}
                color={args.subtitleColor}
            >
                {args.subtitle}
            </Typography>
            <Typography
                size={args.paragraphSize}
                gutter={args.paragraphGutter}
                variant={args.paragraphVariant}
                color={args.paragraphColor}
            >
                {args.paragraph}
            </Typography>
            <Typography
                size={args.paragraphSize}
                gutter={args.paragraphGutter}
                variant={args.paragraphVariant}
                color={args.paragraphColor}
            >
                {args.paragraph}
            </Typography>
        </Shape>
    </ThemeProvider>
);

<Canvas hidden>
    <Story
        name="Headings"
        args={{
            variant: 'h1',
            color: 'primary',
            size: 700,
            useRegular: false,
            text: 'Lorem ipsum dolor sit amet consetetur ...',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Paragraph"
        args={{
            variant: 'body',
            color: 'primary',
            size: 200,
            useRegular: false,
            text: 'Lorem ipsum dolor sit amet consetetur ...',
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Text-Section"
        argTypes={{
            heading: { table: { disable: true } },
            headingSize: {
                control: {
                    type: 'select',
                    options: [25, 50, 75, 100, 200, 300, 400, 500, 600, 700, 1000],
                },
            },
            headingVariant: { control: { type: 'select', options: ['h1', 'h2', 'h3', 'h4', 'h5', 'h6'] } },
            headingGutter: { control: { type: 'radio', options: ['none', 'both', 'top', 'bottom'] } },
            headingColor: { control: { type: 'radio', options: ['primary', 'secondary'] } },
            subtitle: { table: { disable: true } },
            subtitleSize: {
                control: {
                    type: 'select',
                    options: [25, 50, 75, 100, 200, 300, 400, 500, 600, 700, 1000],
                },
            },
            subtitleVariant: { table: { disable: true } },
            subtitleGutter: { control: { type: 'radio', options: ['none', 'both', 'top', 'bottom'] } },
            subtitleColor: { control: { type: 'radio', options: ['primary', 'secondary'] } },
            paragraph: { table: { disable: true } },
            paragraphSize: { control: { type: 'select', options: [25, 50, 75, 100, 200, 300] } },
            paragraphVariant: { table: { disable: true } },
            paragraphGutter: { control: { type: 'radio', options: ['none', 'both', 'top', 'bottom'] } },
            paragraphColor: { control: { type: 'radio', options: ['primary', 'secondary'] } },
        }}
        args={{
            heading: 'Lorem ipsum dolor sit amet',
            headingSize: 500,
            headingVariant: 'h2',
            headingUseRegular: false,
            headingGutter: 'both',
            headingColor: 'primary',
            subtitle:
                'Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam nonumy eirmod tempor invidunt ut labore et dolore magna aliquyam erat, sed diam voluptua.',
            subtitleSize: 300,
            subtitleVariant: 'subtitle',
            subtitleGutter: 'both',
            subtitleColor: 'primary',
            paragraph:
                'Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam nonumy eirmod tempor invidunt ut labore et dolore magna aliquyam erat, sed diam voluptua. At vero eos et accusam et justo duo dolores et ea rebum. Stet clita kasd gubergren, no sea takimata sanctus est Lorem ipsum dolor sit amet. Lorem ipsum dolor sit amet, consetetur sadipscing elitr, sed diam nonumy eirmod tempor invidunt ut labore et dolore magna aliquyam erat, sed diam voluptua. At vero eos et accusam et justo duo dolores et ea rebum. Stet clita kasd gubergren, no sea takimata sanctus est Lorem ipsum dolor sit amet.',
            paragraphSize: 100,
            paragraphVariant: 'body',
            paragraphGutter: 'both',
            paragraphColor: 'primary',
        }}
    >
        {ParagraphTemplate.bind({})}
    </Story>
</Canvas>
