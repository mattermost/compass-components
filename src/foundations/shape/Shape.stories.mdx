import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import GlobalStyle from '../../global-styles/globalStyles';
import Shape from './index';

export const Template = args => (
    <>
        <GlobalStyle />
        <Shape {...args} />
    </>
);

export const defaultArgs = {
    borderRadius: 4,
    elevation: 1,
    component: 'section',
    width: 200,
    height: 100,
    background: '#FFF',
};

export const argTypes = {
    borderRadius: {
        control: {
            type: 'select',
            options: [0, 4, 8, 12, 16, 20, 24, 'circle', 'pill'],
        },
    },
    elevation: {
        control: {
            type: 'select',
            options: [0, 1, 2, 3, 4, 5, 6],
        },
    },
    background: {
        control: 'color',
    },
};

<Meta component={Shape} title="Foundations/Shape" />

<GlobalStyle />

# Shape

---

#### Shape is another important aspect of the visual style. When shapes are applied consistently, a sense of familiarity, trust, and reliability is reinforced.

### Core Shapes

There are 4 core shapes used throughout the Mattermost interface:

### Rectangle

<Shape {...defaultArgs} borderRadius={0} />

### Rounded Rectangle

<Shape {...defaultArgs} borderRadius={12} />

### Pill Shape

<Shape {...defaultArgs} borderRadius={'pill'} />

### Circle

<Shape {...defaultArgs} borderRadius={'circle'} />

<ArgsTable of={Shape} />

<Canvas>
    <Story
        name="Rectangle"
        argTypes={argTypes}
        args={{
            ...defaultArgs,
            borderRadius: 0,
            elevation: 0,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Rounded Rectangle"
        argTypes={argTypes}
        args={{
            ...defaultArgs,
            borderRadius: 16,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Pill"
        argTypes={argTypes}
        args={{
            ...defaultArgs,
            borderRadius: 'pill',
            elevation: 2,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Circle"
        argTypes={argTypes}
        args={{
            borderRadius: 'circle',
            elevation: 3,
            component: 'section',
            width: 300,
            height: 100,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
