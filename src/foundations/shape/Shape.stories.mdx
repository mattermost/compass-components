import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import GlobalStyle from '../../global-styles/globalStyles';
import Shape from './index';
import Grid from '../layout/Grid';

export const Template = args => (
    <>
        <GlobalStyle />
        <Shape {...args} />
    </>
);

export const defaultArgs = {
    border: true,
    borderRadius: 4,
    elevation: 1,
    component: 'section',
    width: 150,
    height: 50,
    background: '#FFF',
};

export const argTypes = {
    borderRadius: {
        control: {
            type: 'select',
            options: [0, 4, 8, 12, 16, 20, 24, 'circle', 'pill'],
        },
    },
    elevation: {
        control: {
            type: 'select',
            options: [0, 1, 2, 3, 4, 5, 6],
        },
    },
    background: {
        control: 'color',
    },
};

<Meta component={Shape} title="Foundations/Shape" />

<GlobalStyle />

# Shape

---

> Shape is another important aspect of the visual style.
> When shapes are applied consistently, a sense of familiarity, trust, and reliability is reinforced.

### Core Shapes

There are 4 core shapes used throughout the Mattermost interface:

<Grid padding={[24, 0]} justify={'stretch'}>
    <Grid row padding={[12, 6]} alignment={'center'}>
        <Grid justify={'center'}>
            <Shape {...defaultArgs} borderRadius={0} />
        </Grid>
        <Grid justify={'center'}>
            <Shape {...defaultArgs} borderRadius={12} />
        </Grid>
        <Grid justify={'center'}>
            <Shape {...defaultArgs} borderRadius={'pill'} />
        </Grid>
        <Grid justify={'center'}>
            <Shape {...defaultArgs} borderRadius={'circle'} />
        </Grid>
    </Grid>
</Grid>

<ArgsTable of={Shape} />

## Rectangle

This is often used for larger, more global containers that extend to the edges of the app window.

## Rounded Rectangle

The rounded rectangle is the most widely used shape for elements like Buttons, Modals, or Popover Menus.

### Default Corner Radius

The default corner radius for a container is 4px.
This is used by Buttons, Icon Buttons, Popover Menus, and many other interface elements throughout the product.

<Grid padding={[24, 0]} justify={'stretch'}>
    <Grid row padding={0} alignment={'center'}>
        <Grid justify={'center'} flex={0}>
            <Shape borderRadius={0} background={'#F7F7F7'} padding={24}>
                <Shape {...defaultArgs} width={156} height={156} borderRadius={4} />
            </Shape>
            <br />
            <strong>4px</strong>
        </Grid>
    </Grid>
</Grid>

###### Other Corner Radiuses

---

export const radiusExampleArgs = {
    ...defaultArgs,
    elevation: 2,
    width: 120,
    height: 120,
};

<Grid padding={[24, 0]} justify={'stretch'}>
    <Grid row padding={0} justify={'space-between'}>
        <Grid justify={'center'} flex={0}>
            <Shape borderRadius={0} background={'#F7F7F7'} padding={24}>
                <Shape {...radiusExampleArgs} borderRadius={8} />
            </Shape>
            <br />
            <strong>8px</strong>
        </Grid>
        <Grid justify={'center'} flex={0}>
            <Shape borderRadius={0} background={'#F7F7F7'} padding={24}>
                <Shape {...radiusExampleArgs} borderRadius={12} />
            </Shape>
            <br />
            <strong>12px</strong>
        </Grid>
        <Grid justify={'center'} flex={0}>
            <Shape borderRadius={0} background={'#F7F7F7'} padding={24}>
                <Shape {...radiusExampleArgs} borderRadius={16} />
            </Shape>
            <br />
            <strong>16px</strong>
        </Grid>
        <Grid justify={'center'} flex={0}>
            <Shape borderRadius={0} background={'#F7F7F7'} padding={24}>
                <Shape {...radiusExampleArgs} borderRadius={20} />
            </Shape>
            <br />
            <strong>20px</strong>
        </Grid>
        <Grid justify={'center'} flex={0}>
            <Shape borderRadius={0} background={'#F7F7F7'} padding={24}>
                <Shape {...radiusExampleArgs} borderRadius={24} />
            </Shape>
            <br />
            <strong>24px</strong>
        </Grid>
    </Grid>
</Grid>

## Pill Shape

The pill shape is essentially a Rounded Rectangle with the radius set to half the height of the container.
This shape is used in a few select places—namely the Mention Badge.

## Circle

The circle shape is mostly reserved for the Avatar component.
This helps distinguish users from other UI elements throughout the product.
There are a few other elements that use the circle shape, like the Unread Badge or the Status Badge.

<Canvas hidden>
    <Story
        name="Rectangle"
        argTypes={argTypes}
        args={{
            ...defaultArgs,
            borderRadius: 0,
            elevation: 0,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Rounded Rectangle"
        argTypes={argTypes}
        args={{
            ...defaultArgs,
            borderRadius: 16,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Pill"
        argTypes={argTypes}
        args={{
            ...defaultArgs,
            borderRadius: 'pill',
            elevation: 2,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Circle"
        argTypes={argTypes}
        args={{
            ...defaultArgs,
            borderRadius: 'circle',
            elevation: 3,
            component: 'section',
            width: 300,
            height: 100,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
