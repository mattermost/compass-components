import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import GlobalStyle from '../../global-styles/globalStyles';
import Shape from './index';

<Meta component={Shape} title="Foundations/Shape" />

export const Template = args => (
    <>
        <GlobalStyle />
        <Shape {...args} />
    </>
);

export const argTypes = {
    borderRadius: {
        control: {
            type: 'select',
            options: [0, 4, 8, 12, 16, 20, 24, 'circle', 'pill'],
        },
    },
    elevation: {
        control: {
            type: 'select',
            options: [0, 1, 2, 3, 4, 5, 6],
        },
    },
};

<Canvas>
    <Story
        name="Rectangle"
        argTypes={argTypes}
        args={{
            borderRadius: 0,
            elevation: 0,
            component: 'section',
            width: 300,
            height: 300,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Rounded Rectangle"
        argTypes={argTypes}
        args={{
            borderRadius: 16,
            elevation: 1,
            component: 'section',
            width: 300,
            height: 300,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Pill"
        argTypes={argTypes}
        args={{
            borderRadius: 'pill',
            elevation: 2,
            component: 'section',
            width: 300,
            height: 100,
        }}
    >
        {Template.bind({})}
    </Story>
    <Story
        name="Circle"
        argTypes={argTypes}
        args={{
            borderRadius: 'circle',
            elevation: 3,
            component: 'section',
            width: 300,
            height: 100,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
