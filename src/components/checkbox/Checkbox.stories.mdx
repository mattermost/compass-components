import { useState } from 'react';
import { Meta, Story, Canvas, Title, Subtitle } from '@storybook/addon-docs/blocks';
import { Utils, defaultArgumentsTableExclusion } from '../../utils';

import Checkbox from './index';
const CheckboxStateful = (args) => {
    const [value, setValue] = useState(false);
    let newState = value === true ? 'on' : 'off';
    const newArgs = {
        ...args,
        state: newState,
    };
    return <Checkbox {...newArgs} onClick={() => setValue(!value)} />;
};
export const Template = (args) => CheckboxStateful(args);

export const defaultArgs = {
    labelText: 'Public',
    state: 'on',
    size: 'md',
    hideLabel: false,
};

export const argTypes = Utils.hideStyledComponentProperties({
    state: {
        control: {
            type: 'select',
            options: ['on', 'off', 'status', 'disabled'],
        },
    },
    size: {
        control: {
            type: 'select',
            options: ['sm', 'md', 'lg'],
        },
    },
    hideLabel: {
        control: {
            type: 'radio',
            options: ['true', 'false'],
        },
    },
});

<Meta
    component={Checkbox}
    title="Components/Checkbox"
    parameters={{
        actions: {
            handles: ['click .Checkbox'],
        },
    }}
/>

<Title>Checkboxes</Title>

<Subtitle>Checkboxes empower users to make multiple selections.</Subtitle>

## Anatomy

Checkboxes are comprised of:

-   Selector
-   Label
    Clicking either the Selector or the Label should toggle the state of the checkbox.

## States

## Animation

## Sizes

export const canvasExampleArgs = {
    ...defaultArgs,
};

<Canvas hidden>
    <Story
        name="Default"
        argTypes={argTypes}
        args={{
            ...canvasExampleArgs,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
