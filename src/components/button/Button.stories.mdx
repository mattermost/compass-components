import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import Button from './Button.tsx';
import {
    BUTTON_SIZES,
    BUTTON_VARIANTS,
    BUTTON_WIDTHS,
    DEFAULT_BUTTON_SIZE,
    DEFAULT_BUTTON_VARIANT,
    DEFAULT_BUTTON_WIDTH,
} from './Button.constants';
import { DEFAULT_ICON_GLYPH, ICON_GLYPHS } from '../icon';
import ThemeProvider from '../../foundations/theme-provider/theme-provider';

<Meta title={'Components/Button'} />

<ThemeProvider>

</ThemeProvider>

<Canvas hidden>
    <Story
        name="Default"
        args={{
            label: 'BUTTON',
            disabled: false,
            destructive: false,
            size: DEFAULT_BUTTON_SIZE,
            variant: DEFAULT_BUTTON_VARIANT,
            width: DEFAULT_BUTTON_WIDTH,
            leadingIcon: null,
            trailingIcon: null,
        }}
        argTypes={{
            size: { control: { type: 'select', options: BUTTON_SIZES } },
            variant: { control: { type: 'select', options: BUTTON_VARIANTS } },
            width: { control: { type: 'select', options: [...BUTTON_WIDTHS, 100, 200, 300, 400] } },
            leadingIcon: { table: { disable: true } },
            trailingIcon: { table: { disable: true } },
            className: { table: { disable: true } },
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Leading Icon"
        args={{
            label: 'BUTTON',
            disabled: false,
            destructive: false,
            size: DEFAULT_BUTTON_SIZE,
            variant: DEFAULT_BUTTON_VARIANT,
            width: DEFAULT_BUTTON_WIDTH,
            leadingIcon: DEFAULT_ICON_GLYPH,
            trailingIcon: null,
        }}
        argTypes={{
            size: { control: { type: 'select', options: BUTTON_SIZES } },
            variant: { control: { type: 'select', options: BUTTON_VARIANTS } },
            width: { control: { type: 'select', options: [...BUTTON_WIDTHS, 100, 200, 300, 400] } },
            leadingIcon: { control: { type: 'select', options: ICON_GLYPHS } },
            trailingIcon: { table: { disable: true } },
            className: { table: { disable: true } },
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
    <Story
        name="Trailing Icon"
        args={{
            label: 'BUTTON',
            disabled: false,
            destructive: false,
            size: DEFAULT_BUTTON_SIZE,
            variant: DEFAULT_BUTTON_VARIANT,
            width: DEFAULT_BUTTON_WIDTH,
            leadingIcon: null,
            trailingIcon: DEFAULT_ICON_GLYPH,
        }}
        argTypes={{
            size: { control: { type: 'select', options: BUTTON_SIZES } },
            variant: { control: { type: 'select', options: BUTTON_VARIANTS } },
            width: { control: { type: 'select', options: [...BUTTON_WIDTHS, 100, 200, 300, 400] } },
            leadingIcon: { table: { disable: true } },
            trailingIcon: { control: { type: 'select', options: ICON_GLYPHS } },
            className: { table: { disable: true } },
        }}
    >
        {(args) => <Button {...args} />}
    </Story>
</Canvas>
