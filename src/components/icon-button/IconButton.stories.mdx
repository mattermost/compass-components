import { Meta, Story, Canvas, ArgsTable, Title, Subtitle } from '@storybook/addon-docs/blocks';

import IconButton from './IconButton';
import { ICON_BUTTON_SIZES } from './IconButton.constants';

<Meta component={IconButton} title="Components/IconButton" />

<Title>Icon Button</Title>

## Component API

<ArgsTable of={IconButton} />

export const Template = (args) => {
    return <IconButton {...args} />;
};

<Canvas hidden>
    <Story
        name="Default"
        args={{
            size: 'standard',
        }}
        argTypes={{
            size: {
                control: {
                    type: 'select',
                    options: ICON_BUTTON_SIZES,
                },
            },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
