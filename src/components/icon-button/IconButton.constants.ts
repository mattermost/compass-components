import { TIconButtonSize } from './IconButton.types';

const ICON_BUTTON_SIZES: TIconButtonSize[] = [
    'xsmall',
    'small-compact',
    'small',
    'standard',
    'large',
];

export { ICON_BUTTON_SIZES };
