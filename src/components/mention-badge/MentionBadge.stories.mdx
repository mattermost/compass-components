import { Meta, Story, Canvas, Title, Subtitle } from '@storybook/addon-docs/blocks';

import MentionBadge from './index';
import { MENTIONBADGE_SIZES } from './MentionBadge.constants';
import { Utils } from '../../shared';

export const mentionBadgeDefaultArgs = {
    mentions: 1,
    size: 'md',
};

export const unreadBadgeDefaultArgs = {
    mentions: 0,
    size: 'md',
};

export const mentionBadgeArgTypes = {
    size: {
        options: MENTIONBADGE_SIZES,
        control: {
            type: 'select',
            labels: {
                xs: 'x-small',
                sm: 'small',
                md: 'medium',
                lg: 'large',
                xl: 'x-large',
                xxl: 'xx-large',
            },
        },
    },
    mentions: {
        control: {
            type: 'range',
            min: 1,
            max: 100,
            step: 1,
        },
    },
    mentionLimit: {
        control: {
            type: 'range',
            min: 10,
            max: 100,
            step: 10,
        },
    },
    inverted: {
        control: {
            type: 'boolean',
        },
    },
    background: {
        control: {
            type: 'color',
        },
    },
    ...Utils.hideComponentProperties(['width']),
};

<Meta component={MentionBadge} title="Components/MentionBadge" parameters={{ status: 'stable' }} />

<Title>Badges</Title>

<Subtitle>
    Badges are small components typically used to indicate a numerical value or the status of an
    item to the user.
</Subtitle>

## Mention Badges

Mention Badges are used to show a numerical count of mentions that a user has received. It can be found in
the Channel Sidebar as well as the App Header Bar.

In cases where there is a number larger than 99 displayed in the badge, we limit the badge display to 99+

## Unread Badges

Unread Badges are smaller badges used to inform the user that there are unread messages in a particular
Channel or Team. Unread Badges do not display a count.

## Status Badges

Status Badges are used to indicate the status of a particular user and are usually paired with an avatar
or username

<Canvas hidden>
    <Story name="Mention Badges" args={mentionBadgeDefaultArgs} argTypes={mentionBadgeArgTypes}>
        {(args) => <MentionBadge {...args} />}
    </Story>
    <Story name="Unread Badges" args={unreadBadgeDefaultArgs} argTypes={mentionBadgeArgTypes}>
        {(args) => <MentionBadge {...args} />}
    </Story>
</Canvas>
