import { useState } from 'react';
import { Meta, Story, Canvas, Title, Subtitle, ArgsTable } from '@storybook/addon-docs/blocks';
import { DEFAULT_RADIO_SIZE, RADIO_SIZE_MAPPING, RADIO_SIZES } from './Radio.constants';
import { Utils } from '../../shared';
import Grid, { Spacing } from '../../foundations/layout';
import Text from '../text';
import radioAnatomyImg from '../../assets/images/example-radio-anatomy.png';
import radioAnimationGif from '../../assets/images/example-radio-animation.gif';
import radioImg from '../../assets/images/example-radio.png';
import { DEFAULT_ARGUMENT_STABLE_EXCLUSION } from '../../shared/constants';
import Radio from './index';

export const defaultArgs = {
    label: 'Label',
    size: DEFAULT_RADIO_SIZE,
};

<Meta component={Radio} title="Components/Radio" />

<Title>Radio buttons</Title>

<Subtitle>
    Radio buttons are recommended for a single selection input, where the user can only select one
    option at a time.
</Subtitle>

## Anatomy

Radio buttons are comprised of:

-   Selector
-   Label
    Clicking either the Selector or the Label should toggle the state of the radio button.

<img src={radioAnatomyImg} alt={'radio-anatomy'} />

## States

<Grid row justify={'space-between'}>
    <Grid justify={'center'}>
        <Radio {...defaultArgs} disabled={true} />
        <br />
        <Text element={'p'} size={200} margin={'none'}>
            <strong>Disabled</strong>
        </Text>
    </Grid>
    <Grid justify={'center'}>
        <Radio {...defaultArgs} hasError={true} />
        <br />
        <Text element={'p'} size={200} margin={'none'}>
            <strong>Error</strong>
        </Text>
    </Grid>
    <Grid justify={'center'}>
        <Radio {...defaultArgs} />
        <br />
        <Text element={'p'} size={200} margin={'none'}>
            <strong>Off</strong>
        </Text>
    </Grid>
    <Grid justify={'center'}>
        <Radio {...defaultArgs} checked={true} />
        <br />
        <Text element={'p'} size={200} margin={'none'}>
            <strong>On</strong>
        </Text>
    </Grid>
</Grid>

Once an item is selected, if the user clicks to select a different item, this automatically deselects the initial selection.

## Animation

<img src={radioAnimationGif} alt={'radio-animation'} />

## Sizes

<Grid row justify={'space-between'}>
    <Grid justify={'center'}>
        <Radio label={'Label'} size={'sm'} checked={true} />
        <br />
        <Text element={'p'} size={200} margin={'none'}>
            <strong>Small</strong>
        </Text>
    </Grid>
    <Grid justify={'center'}>
        <Radio label={'Label'} size={'md'} checked={true} />
        <br />
        <Text element={'p'} size={200} margin={'none'}>
            <strong>Medium</strong>
        </Text>
    </Grid>
    <Grid justify={'center'}>
        <Radio label={'Label'} size={'lg'} checked={true} />
        <br />
        <Text element={'p'} size={200} margin={'none'}>
            <strong>Large</strong>
        </Text>
    </Grid>
</Grid>

## Example

<img src={radioImg} alt={'radio-example'} />

## Dependencies

<ArgsTable of={Radio} exclude={[...DEFAULT_ARGUMENT_STABLE_EXCLUSION]} />

<Canvas hidden>
    <Story
        name="Default"
        argTypes={Utils.hideStyledComponentProperties({
            label: { control: { type: 'text' } },
            size: { control: { type: 'radio', options: RADIO_SIZES } },
            hasError: { control: { type: 'boolean' } },
            disabled: { control: { type: 'boolean' } },
            checked: { control: { type: 'boolean' } },
            className: { table: { disable: true } },
            onClick: { action: 'clicked', table: { disable: true } },
        })}
        args={{
            label: 'Default Radio',
            size: DEFAULT_RADIO_SIZE,
        }}
    >
        {(args) => <Radio {...args} />}
    </Story>
</Canvas>
