import { Meta, Story, Canvas, ArgsTable, Title } from '@storybook/addon-docs/blocks';

import Icon from './Icon';
import { DEFAULT_ICON_GLYPH, DEFAULT_ICON_SIZE, ICON_GLYPHS, ICON_SIZES } from './Icon.constants';
import { Utils } from '../../shared';
import { DEFAULT_ARGUMENTSTABLE_EXCLUSION } from '../../shared/constants';

<Meta component={Icon} title="Components/Icon" />

<Title>Icon</Title>

## Component API

<ArgsTable
    of={Icon}
    exclude={[...DEFAULT_ARGUMENTSTABLE_EXCLUSION, 'data-size', 'data-glyph', 'aria-label']}
/>

export const Template = (args) => {
    return <Icon {...args} />;
};

<Canvas hidden>
    <Story
        name="Default"
        args={{
            size: DEFAULT_ICON_SIZE,
            glyph: DEFAULT_ICON_GLYPH,
        }}
        argTypes={{
            size: {
                options: ICON_SIZES,
                control: {
                    type: 'select',
                },
            },
            glyph: {
                options: ICON_GLYPHS,
                control: {
                    type: 'select',
                },
            },
            ...Utils.hideComponentProperties(['aria-label']),
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
