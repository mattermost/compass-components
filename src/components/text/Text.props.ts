import { TTextSize, TTextVariant, TTextWeight } from './Text.types';

type PText = {
    className?: String;
    size?: TTextSize;
    variant?: TTextVariant;
    weight?: TTextWeight;
};

export { PText };
