import { Meta, Story, Canvas, ArgsTable, Title } from '@storybook/addon-docs/blocks';

import Image from './Image';
import { DEFAULT_IMAGE_SOURCE, DEFAULT_IMAGE_SIZE, IMAGE_SIZES } from './Image.constants';
import { Utils } from '../../shared';
import { DEFAULT_ARGUMENT_STABLE_EXCLUSION } from '../../shared/constants';
import { SHAPE_BORDER_RADII } from '../../foundations/shape/Shape.constants';

<Meta component={Image} title="Components/Image" />

<ArgsTable of={Image} exclude={[...DEFAULT_ARGUMENT_STABLE_EXCLUSION]} />

<Canvas hidden>
    <Story
        name="Default"
        args={{
            source: DEFAULT_IMAGE_SOURCE,
            radius: 'circle',
            size: DEFAULT_IMAGE_SIZE,
        }}
        argTypes={Utils.hideStyledComponentProperties({
            size: { control: { type: 'select', options: IMAGE_SIZES } },
            width: { control: { type: 'range', min: 1, max: 1000, step: 1 } },
            height: { control: { type: 'range', min: 1, max: 1000, step: 1 } },
            radius: { control: { type: 'select', options: SHAPE_BORDER_RADII } },
            source: { control: { type: 'text' } },
            thumbnail: { control: { type: 'boolean' } },
            alt: { table: { disable: true } },
            className: { table: { disable: true } },
        })}
    >
        {(args) => <Image {...args} />}
    </Story>
</Canvas>
