import { Meta, Story, Canvas, ArgsTable, Title, Subtitle } from '@storybook/addon-docs/blocks';

import Heading from './Heading';
import {
    HEADING_SIZES,
    HEADING_MARGINS,
    HEADING_ELEMENTS,
    HEADING_WEIGHTS,
    HEADING_COLORS,
    DEFAULT_HEADING_SIZE,
    DEFAULT_HEADING_WEIGHT,
    DEFAULT_HEADING_MARGIN,
    DEFAULT_HEADING_COLOR,
    DEFAULT_HEADING_ELEMENT,
} from './Heading.constants';

<Meta component={Heading} title="Components/Heading" />

<Title>Heading</Title>

## Component API

<ArgsTable of={Heading} exclude={['as', 'ref', 'forwardedAs', 'theme']} />

export const Template = (args) => {
    const { text } = args;
    return <Heading {...args}>{text}</Heading>;
};

<Canvas hidden>
    <Story
        name="Default"
        args={{
            text: 'Lorem Ipsum Dolor',
            element: DEFAULT_HEADING_ELEMENT,
            size: DEFAULT_HEADING_SIZE,
            weight: DEFAULT_HEADING_WEIGHT,
            margin: DEFAULT_HEADING_MARGIN,
            color: DEFAULT_HEADING_COLOR,
        }}
        argTypes={{
            size: { control: { type: 'select', options: HEADING_SIZES } },
            element: { control: { type: 'select', options: HEADING_ELEMENTS } },
            weight: { control: { type: 'radio', options: HEADING_WEIGHTS } },
            margin: { control: { type: 'radio', options: HEADING_MARGINS } },
            color: { control: { type: 'radio', options: HEADING_COLORS } },
            className: { table: { disable: true } },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>
